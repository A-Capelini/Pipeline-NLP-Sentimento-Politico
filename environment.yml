name: opinate_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - matplotlib
  - pip
  - pip:
      - mysql-connector-python
      - sqlalchemy
      - python-dotenv
      - google-genai
      - Faker
      - streamlit
      - plotly
      - wordcloud
